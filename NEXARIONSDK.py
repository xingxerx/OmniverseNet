import NexarionLib
from QuantumEntanglement import Entangle

class OmniverseNetActivator:
  # Corrected constructor name from _init_ to __init__
  def __init__(self, creator_credentials):
    # Removed the non-breaking space (U+00A0) after '='
    # Assigning the value from the argument for completeness
    self.creator_name = creator_credentials
    # If you don't want to assign it yet, you can do:
    # self.creator_name = None
